apiVersion: v1
kind: Service
metadata:
  name: svc-php-fpm-exporter
  namespace: dev
spec:
  # type: NodePort
  ports:
    - port: 80
      targetPort: 9253
      # nodePort: 30006
  selector:
    web: nginx-php
    tier: front-end
